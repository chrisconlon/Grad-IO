% Minimal wrapper to use the canonical shared preamble
% Set per-file class options here if you need (e.g. handout, notes)
\def\beamerclassoptions{[10pt,aspectratio=169]}
\input{../../resources/preamble.tex}

% Folder-specific tweaks can go below. Keep changes minimal to avoid
% duplicating definitions present in the shared preamble.
% Unified wrapper that uses the central shared preamble for consistent styling.
